require 'spec_helper'

describe ActiveEnumerable do
  it 'has a version number' do
    expect(ActiveEnumerable::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
